// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // Quando comprar o domínio, insira a URL oficial aqui
  site: 'https://www.seudominio.com.br',
});
